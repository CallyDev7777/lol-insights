apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lol-insights-ingress
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lol-insights-service
                port:
                  number: 80